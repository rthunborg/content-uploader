# Deferred Work Ledger

- source_spec: `_bmad-output/implementation-artifacts/spec-1-2-data-layer-account-organization-foundation.md`
  summary: The organization tables (campaigns, themes, asset_themes, asset_campaigns) have no fast-gate (DB-less) schema-config unit tests asserting their FK on-delete actions and CHECK/uniqueness constraints, unlike profiles which asserts its account-state check via getTableConfig.
  evidence: profiles.test.ts asserts the `profiles_account_state_check` name through `getTableConfig`, but campaigns/themes/join tables have no co-located unit test; the FK on-delete actions (theme_id `set null`, campaign_id/theme_id `restrict`, asset_id `cascade`), the `campaigns_date_order_check`, and `themes_name_unique` are verified only inside `organization.integration.test.ts`, which is `describe.skip` unless `TEST_DATABASE_URL` is set and no CI wires it. A schema regression (e.g. flipping an on-delete action or reintroducing `deleted`) would ship green under the default `npm test`. Mirroring the profiles getTableConfig pattern would catch schema-level regressions in the fast gate.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-fleet-deck-design-tokens-themed-component-base.md`
  summary: The Fleet Deck primitives' computed-style guarantees (2 px focus outline width, 44 px rendered target boxes, 16 px control font-size, approved-pairing contrast of the actually-rendered colors, and the phone-viewport single-column/no-bulk-controls collapse) are verified only via source-string and Tailwind class-name matching, never via a real layout/paint engine — deferrable until these primitives are mounted on navigable feature pages where browser-level (Playwright) assertions become possible.
  evidence: The spec Approach states tests should "observe the rendered public surface and accessibility contract," yet `fleet-deck.test.ts` asserts against raw `globals.css`/`layout.tsx` text and the AA-contrast check computes from hex literals hardcoded in the test body rather than from the token layer; the shell/button tests use `renderToStaticMarkup` + `toContain` on class strings (`min-h-11`, `focus-visible:outline-2`, `max-w-[1440px]`, `hidden md:block`) with no viewport or computed style. This is the same class of gap that let the font-variable-scope bug (Inter loaded but bound on `<body>` while consumed on `<html>`, so it never rendered) pass a green suite before this review caught and fixed it. The prior Auto Run Result already acknowledged this as residual risk ("guarded through generated classes and emitted CSS rather than screenshot/measured browser tests because these primitives are not yet mounted on navigable feature pages"). @playwright/test is already a dependency, so once a feature screen consumes these primitives, computed-style/e2e assertions can close the gap.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-fleet-deck-design-tokens-themed-component-base.md`
  summary: The `dialogDestructive` button variant pairs white (`--surface` `#ffffff`) text on Core Red (`--destructive` `#e41f1f`), which computes to ~4.48:1 contrast — just below the 4.5:1 WCAG AA floor for its 16 px semibold (normal-weight) label — and neither approved palette foreground (white nor Black `#1a1a1a`, ~3.89:1) clears AA on Core Red; deferrable because the variant is dialog-only per the intent and is not yet mounted on any surface, so no current user is affected.
  evidence: `src/components/ui/button.tsx` defines `dialogDestructive: "bg-destructive text-surface …"` (white on `#e41f1f`). Recomputing the WCAG contrast: L(white)=1.0, L(#e41f1f)≈0.1845, ratio=(1.0+0.05)/(0.1845+0.05)≈4.48, under 4.5 for normal text (16 px `font-semibold`, weight 600, is not WCAG "large text": needs ≥18.66 px bold or ≥24 px). Black text on Core Red is worse (~3.89:1). The `fleet-deck.test.ts` AA check only asserts the four dark-on-light pairings and never this white-on-red one, so both prior review passes missed it. The intent's Block-If ("approved palette pairings cannot meet WCAG AA without changing a locked token") is implicated, but the intent also scopes destructive presentation to confirmation dialogs, none of which exist yet — so the pairing is not observable by any user until such a dialog is built. Resolution (large-text sizing for the confirm label, or a locked-token decision on Core Red) belongs to the story that first mounts a destructive confirmation dialog.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-4-magic-link-authentication-the-single-front-door.md`
  summary: The `/auth/login` magic-link request discloses whether an account exists — an existing user yields the "check your inbox" success copy (`AUTH_COPY.linkSent`) while `signInWithOtp` returning an error (e.g. an unknown email under `shouldCreateUser: false`) yields the distinct `AUTH_COPY.requestFailed` copy — contrary to the intent's enumeration-safety criterion.
  evidence: `src/app/auth/auth-flow.ts` `requestMagicLinkWithClient` returns `{ ok: false, message: AUTH_COPY.requestFailed }` on any provider error and `{ ok: true }` (→ `AUTH_COPY.linkSent` in `src/app/auth/login/actions.ts`) on success, so a caller can distinguish a registered address from an unknown one by the rendered Swedish response. Story 1.4's acceptance criteria and Boundaries require an "enumeration-safe" response that "does not disclose whether an account exists," but this request path is unchanged by the Story 1.4 diff (only the `LoginState`/`INITIAL_LOGIN_STATE` import moved), and the e2e exercises only the existing-user branch — so it is a pre-existing defect surfaced incidentally by this review. Collapsing both outcomes to a single generic confirmation message (and covering the unknown-email branch in tests) would close it.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-4-magic-link-authentication-the-single-front-door.md`
  summary: The invite email template assumes the provider's `.RedirectTo` always carries a query string; an administrator invitation issued with a query-less `redirectTo` produces a malformed link (`http://host&token_hash=...&type=invite`) whose token collapses into the path and never reaches `/auth/confirm`.
  evidence: `supabase/templates/invite.html` builds the href as `{{ .RedirectTo }}&amp;token_hash={{ .TokenHash }}&amp;type=invite`, valid only when `.RedirectTo` already contains a `?`. The magic-link path is safe because `buildConfirmationUrl` always emits `?next=`, but invite `redirectTo` is supplied by the (future) admin caller; the only current caller is the e2e test, which happens to pass `?next=%2F`. Invitation onboarding is owned by later stories, so no production caller is reachable today, but the template will silently break the invite journey for any query-less `redirectTo`. A delimiter-robust template (or a guarantee that invite `redirectTo` always carries a query) plus a rendered-href assertion would resolve it.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-4-magic-link-authentication-the-single-front-door.md`
  summary: The Story 1.4 headline guarantees that only the Playwright e2e suite exercises — real single-use enforcement, 15-minute expiry, and the accessible recovery CTA (44 px target + visible focus outline) — have no automated/CI execution path, so they can regress undetected under the default `npm test`.
  evidence: `vitest.config.ts` excludes `e2e/**`, and both `playwright.config.ts` and `e2e/fixtures/auth.ts` hard-require a manually started local Supabase (`npx supabase status`, throwing otherwise). The repo has no CI workflow (`.github/` does not exist). Consequently a change that removes the `.primary-link` focus outline / `min-height` in `globals.css` or breaks the single-use redirect keeps `vitest run` green; only a human remembering to run Playwright against a live stack catches it. Wiring `playwright test` (with `supabase start`) into the project's verification gate — or documenting it as a mandatory pre-merge step — would close the gap. This is a project-level CI gap not caused by Story 1.4 (the story has no mandate to establish CI).

- source_spec: `_bmad-output/implementation-artifacts/spec-1-5-dal-choke-point-auth-contexts-role-separation-revocation.md`
  summary: The production `AUTH_REQUIRED`-vs-`SESSION_REVOKED` derivation in `defaultGetAuth` — capturing `getSession()` evidence before `getUser()` network validation over the real SSR cookie client — is exercised by no test, so a regression that trusts `getSession()` (dropping the `getUser()` validation, an auth bypass) would ship green under `npm test`.
  evidence: `src/lib/auth.ts` wires the exported guards with `defaultGetAuth` (line 83), but `src/lib/auth.test.ts` injects a substitute `getAuth: vi.fn()` via `createAuthGuards`, and `src/lib/auth/revocation.integration.test.ts` builds its own inline `getAuth` over in-memory `supabase-js` clients rather than the SSR cookie client — grep confirms `defaultGetAuth` is referenced only at its wiring line and by no test. The e2e (`e2e/auth-magic-link.spec.ts`) only covers happy-path and invited/deactivated states; it never revokes a live session and re-hits the app, so the "locally present session + failed `getUser()` → `SESSION_REVOKED`" mapping is unverified end-to-end. Story 1.5's intent explicitly defers this ("SESSION_REVOKED / 401 once detection contract is resolved"); an integration test that drives the exported `requireUser` against a globally-revoked session (rather than a reconstructed `getAuth`) would close it in the story that resolves the detection contract.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-5-dal-choke-point-auth-contexts-role-separation-revocation.md`
  summary: The all-device revocation primitive's production default calls `admin.signOut(userId, "global")`, but supabase-js's global logout endpoint is keyed by a user JWT rather than a user id, and this real service-role call path is verified only against a mock and a translating adapter that discards the userId — so the primitive's real invalidation contract is unproven.
  evidence: `src/lib/auth/revocation.ts` defaults to `createAdminSupabaseClient().auth.admin.signOut(userId, "global")`; `revocation.test.ts` asserts against a mock accepting `(userId, "global")`, and `revocation.integration.test.ts` injects an adapter whose `signOut(_userId, scope)` discards the userId and calls `service.auth.admin.signOut(jwt, scope)` — the test's own comment states supabase-js "exposes the global logout endpoint by user JWT rather than user id." The spec's Auto Run Result already records this as a residual risk ("the installed Supabase JS admin sign-out endpoint accepts a user JWT rather than a UUID … later mutation-owner stories must provide the production token-acquisition adapter"). Stories 2.4/2.5/3.3 own the mutation callers; the story that first wires a production caller must supply and test the real token-acquisition adapter so the userId→global-invalidation path is proven against live Supabase.

### DW-1: Follow-up review still recommended for 1-5-dal-choke-point-auth-contexts-role-separation-revocation after the damping cap was spent
origin: review-budget-followup
source_spec: `spec-1-5-dal-choke-point-auth-contexts-role-separation-revocation.md`
severity: low
reason: The follow-up-review damping cap (limits.max_followup_reviews = 1) was spent with the story finalized (status: done, verify green) while the review pass still recommended an independent follow-up. The work was committed by bmad-loop run 20260713-170234-9e4b; this entry preserves the lingering recommendation for a deliberate later review.
status: open

- source_spec: `_bmad-output/implementation-artifacts/spec-1-6-audit-event-emitter-registry.md`
  summary: The entire audit-compliance guarantee set — table RLS/FORCE-RLS, the immutability trigger's own reject branch, transaction-atomic rollback of mutation+audit row, the CHECK constraint admitting every registered event type, and the six-month retention scoped to `audit_events` alone — is proven only by two DB-gated integration suites that become `describe.skip` when no local Supabase is reachable, and no CI runs them, so these guarantees regress green under the default `npm test`.
  evidence: `src/shared/audit.integration.test.ts` and `src/db/schema/audit.integration.test.ts` both set `const describeDatabase = databaseUrl ? describe.sequential : describe.skip`, where `databaseUrl` comes from `TEST_DATABASE_URL` or a synchronous `npx supabase status` probe (whose `/^DB_URL=…/` regex silently yields `undefined` — hence skip — if the CLI output format changes). The always-on tests only prove *shape*: the type-only client-rejection and JSON-safety proofs live in `@ts-expect-error` directives inside `if (false)` dead blocks (`src/shared/audit.test.ts`, `src/db/audit-types.test.ts`) that `vitest run` transpiles away (verified only by the separate `npm run typecheck` gate), and `src/db/schema/audit.test.ts` compares the Drizzle enum to the registry (TS-to-TS) without touching Postgres. The repo has no `.github/workflows`, so RLS removal, a dropped immutability trigger, a `service_role` UPDATE grant, or registry-vs-migration CHECK drift (a new `AUDIT_EVENT_TYPES` member without a matching migration → runtime `23514`) all ship undetected unless a developer manually runs local Supabase. This is a pre-existing project-wide convention, not a Story 1.6 defect: five integration suites across stories 1.2–1.6 share the same `describe.skip`/`localDatabaseUrl` gating (`localDatabaseUrl` is duplicated verbatim across three of them), and CI/quality-pipeline hardening is deliberately the final phase in the architecture's build order, so this story has no mandate to establish it. Wiring `vitest` integration + Playwright runs (with `supabase start`) into a CI gate — or making the integration suites fail-loud rather than skip when no database is configured — would close the gap for every audited subsystem at once.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-7-ci-cd-pipeline-error-logging-seam.md`
  summary: Provider error objects can carry enumerable operational fields such as status, request id, and response detail that the shared logger currently drops when serializing `Error` instances.
  evidence: `src/shared/logger.ts` serializes only name, message, stack, and cause for `Error` values; this behavior predates Story 1.7, while provider errors commonly attach diagnostic fields needed for incident investigation. A future focused logger-hardening change should include safe/redacted enumerable fields without exposing provider payload secrets.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-2-invite-ambassadors-by-email.md`
  summary: A freshly invited ambassador can be absent from the roster's first page after `router.refresh()`, because `listAmbassadors` paginates ordered by `asc(profiles.id)` and the id is a random Supabase auth UUID — so with more than 25 ambassadors the new row lands at a random page position, weakening the intent's "the roster shows the invited ambassador" acceptance criterion.
  evidence: `src/features/ambassadors/dal/admin.ts:28` orders by `asc(profiles.id)` with a UUID cursor; the invited profile's id is the auth user id (a v4 UUID), so its first-page probability is ~25/(N+1). The `account.invited` success message gives the admin unambiguous confirmation, and the e2e (`e2e/ambassador-invite.spec.ts`) only asserts visibility against a small test roster (<25 rows), which masks the production behavior. Root cause is the roster's id-ordered pagination contract established in Story 2.1, surfaced incidentally now that Story 2.2 adds a UI path to create ambassadors. Resolution (e.g. ordering the roster by `invitedAt`/`createdAt` desc with a compatible cursor, or surfacing the newly invited row explicitly after success) touches the Story 2.1 pagination design and belongs to a focused roster-ordering change.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-2-invite-ambassadors-by-email.md`
  summary: The `inviteAmbassador` orchestration has no integration coverage that runs in CI — the orphan-compensation (`admin.deleteUser`) path and the duplicate-check SQL's `auth.users` branch (the branch that actually enforces "never invite admin identities" and turns orphan auth identities into `CONFLICT`) are verified only against whole-cloth mocks, so a regression to either would ship green.
  evidence: `src/features/ambassadors/dal/admin.invite.test.ts` mocks `getDatabase`, `execute`, `transaction`, and the Supabase admin client, so the compensation assertions only prove `deleteUser` was *called*, never that a real auth user is removed and no profile row persists; and the mocked `execute` returns a single `{exists}` boolean, so neither branch of the `union all` duplicate SQL is independently exercised. The e2e duplicate case has a `profiles` row, which short-circuits `exists` before the `auth.users` branch decides. The only DAL integration suite (`src/features/ambassadors/dal/admin.integration.test.ts`) covers `listAmbassadors`/`getProfileForAdmin`, is gated on `TEST_DATABASE_URL` with no `supabase status` fallback (unlike `audit.integration.test.ts`), and CI sets no such env — so it is silently `describe.skip` in CI. This is the same project-wide integration-in-CI gap tracked for stories 1.2–1.6; a compensation/duplicate integration test using the `supabase status` DB-URL fallback would close the invite-specific portion.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-2-invite-ambassadors-by-email.md`
  summary: `scripts/create-admin.ts` — the only mechanism that provisions the first admin identity, and therefore a prerequisite for the entire invite flow — still reads the undocumented env names `SUPABASE_URL` / `SUPABASE_SERVICE_ROLE_KEY` with `?? ""` fallbacks, so an operator whose `.env` matches `.env.example` silently gets `createClient("", "")` instead of a loud configuration error.
  evidence: `scripts/create-admin.ts:3` builds its default client from `process.env.SUPABASE_URL ?? ""` and `process.env.SUPABASE_SERVICE_ROLE_KEY ?? ""`; neither name appears in `.env.example`, which documents `NEXT_PUBLIC_SUPABASE_URL` and `SUPABASE_SECRET_KEY` — the canon declared in `_bmad-output/planning-artifacts/architecture.md` for `src/lib/supabase/{browser,server,admin}.ts`. The divergence is pre-existing (`.env.example` already carried `SUPABASE_SECRET_KEY` before Story 2.2); what this story changes is that renaming the sibling factory `src/lib/supabase/admin.ts` to the canonical names removed the last other in-code reader of the old ones, leaving `create-admin.ts` the sole holdout. No test would ever catch it: all three cases in `scripts/create-admin.test.ts` call `provisionAdmin(email, fixture.client as never)` with an injected client, so the default-parameter expression is never evaluated, and the `?? ""` fallbacks suppress the throw that would otherwise reveal the misconfiguration. Resolution (align the script to the canonical names and add a test that stubs `createClient` and asserts which env names the default client is built from) is an operational-script change outside this story's invite surface.

### DW-2: Follow-up review still recommended for 2-2-invite-ambassadors-by-email after the damping cap was spent
origin: review-budget-followup
source_spec: `spec-2-2-invite-ambassadors-by-email.md`
severity: low
reason: The follow-up-review damping cap (limits.max_followup_reviews = 1) was spent with the story finalized (status: done, verify green) while the review pass still recommended an independent follow-up. The work was committed by bmad-loop run 20260713-170234-9e4b; this entry preserves the lingering recommendation for a deliberate later review.
status: open

- source_spec: `_bmad-output/implementation-artifacts/spec-2-3-maintain-ambassador-contact-details.md`
  summary: `src/features/ambassadors/components/invite-form.tsx` renders server-supplied `error.fields` values without the runtime string check that Story 2.3 added to its sibling `contact-form.tsx`, so a non-string field value from the invite endpoint would be passed to React as a child and crash the roster page.
  evidence: `invite-form.tsx:9` does `throw new InviteRequestError(body.error?.code, body.error?.fields)` with `fields` typed as `FieldErrors` but never validated at runtime; `invite-form.tsx:14` writes it straight into `setErrors` and `:16` renders `{errors[field]}`. Story 2.3 introduced `sanitizeFieldErrors` in `contact-form.tsx:18-30` for exactly this class, applied at `:59`, and pinned it with the `contact-form.test.tsx` case "renders only string messages from the server field envelope"; `grep -n "fields" src/features/ambassadors/components/invite-form.test.tsx` returns nothing, so the invite form has no such coverage. This is pre-existing (invite-form shipped in Story 2.2) and not reachable today because `/api/ambassadors` returns Zod-flattened string arrays, making it defense-in-depth rather than a live defect. Resolution: lift `sanitizeFieldErrors` to a shared module next to the schemas and adopt it in both forms, with the stubbed-envelope test added to `invite-form.test.tsx`.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-3-maintain-ambassador-contact-details.md`
  summary: `src/db/schema/organization.integration.test.ts` was not migrated to the local `supabase status` DB-URL discovery that Story 2.3 applied to the other five integration suites, so it remains `describe.skip` on every host and in CI; it is also the one file whose fixtures could race the roster membership assertions if it were ever enabled as-is.
  evidence: `organization.integration.test.ts:4-5` still reads `process.env.TEST_DATABASE_URL` only, while `admin.integration.test.ts`, `db/schema/audit.integration.test.ts`, `db/schema/rls.integration.test.ts`, `lib/auth/revocation.integration.test.ts`, and `shared/audit.integration.test.ts` now fall back to `npx supabase --profile supabase/cli-profile.yaml status --output env`. `.github/workflows/ci.yml` never sets `TEST_DATABASE_URL`, so the organization schema suite (the sole verification of the campaigns/themes FK on-delete actions and CHECK constraints, per the Story 1.2 entry above) has never executed in CI. Migrating it is not a free change: it inserts `auth.users` rows with no `raw_app_meta_data` plus matching `profiles` rows, and `vitest.config.ts` leaves file parallelism on, so once discovered it would run concurrently with `admin.integration.test.ts` against the same database and make any full-set roster assertion flaky. Resolution: migrate it to the shared discovery helper and isolate its fixtures (or serialize the DB-backed files) in the same change.

### DW-3: Follow-up review still recommended for 3-1-versioned-terms-tamper-evident-acceptance-store after the review budget was exhausted
origin: review-budget-followup
source_spec: `spec-3-1-versioned-terms-tamper-evident-acceptance-store.md`
severity: low
reason: Review budget (3 cycles) was exhausted with the story finalized (status: done, verify green) while the review pass kept recommending an independent follow-up. The work was committed by bmad-loop run 20260716-165759-33c2; this entry preserves the lingering follow-up recommendation for a deliberate later review.
status: open

- source_spec: `_bmad-output/implementation-artifacts/spec-3-1-versioned-terms-tamper-evident-acceptance-store.md`
  summary: The consent PII envelope scheme has no key-rotation or wrapped-key versioning path, so rotating `CONSENT_PII_KEK` (or `ACCEPTANCE_HMAC_KEY`) would make every existing wrapped data key undecryptable and break `appendAcceptance` for returning users.
  evidence: `src/features/consent/crypto.ts` (`createUserDataKey`/`unwrapUserDataKey`) wraps each per-user data key with a single env-held KEK using AAD `consent-user-key-v1:${userId}` and no key-id/version, and `consent_pii_keys` stores only ciphertext/nonce/tag with no key-version column. There is no re-wrap/migration routine. A KEK rotation (compromise or policy) therefore either bricks decryption of all historical identity snapshots or requires an unplanned bulk re-encryption. Story 3.1's intent keeps keys outside Postgres but is silent on rotation; a focused change should add a wrapped-key version/key-id column plus a re-wrap procedure so rotation is possible without data loss.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-1-versioned-terms-tamper-evident-acceptance-store.md`
  summary: The acceptance-record/head HMAC canonicalization, key parsing, and constant-time compare are hand-duplicated between `src/features/consent/crypto.ts` and `worker/jobs/verify-acceptance-chain.ts`, so a future edit to one canonical payload that is not mirrored in the other silently splits the writer's and the nightly verifier's definition of "the signed bytes."
  evidence: `worker/jobs/verify-acceptance-chain.ts` re-implements the `["acceptance-record-v1", ...]` / `["acceptance-chain-head-v1", ...]` arrays, the strict base64 key parse, and `equal()` independently of `crypto.ts` (the worker eslint boundary blocks importing `src/features`). They currently match byte-for-byte only by manual coordination; divergence would make the verifier flag every valid record as `acceptance_chain.integrity_failed` (false critical storm) or accept records the writer never signed. A cross-implementation parity test (or a shared boundary-safe canonicalization module) would pin the two definitions together.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-1-versioned-terms-tamper-evident-acceptance-store.md`
  summary: The consent-currency provider loads and HMAC-reverifies the entire append-only ledger on every hot-path check, and the nightly verifier's pgmq visibility timeout (60s) equals its poll interval, so both verification paths degrade without bound as the immutable ledger grows.
  evidence: `src/features/consent/dal/consent-status.ts` selects `acceptance_records order by chain_position` with no LIMIT and passes all rows to `verifyAcceptanceLedger` (O(N) rows + O(N) HMACs) for a single per-user boolean, on the auth hot path via `productionConsentStatusProvider`; the table is append-only (UPDATE/DELETE trigger-blocked) so N only grows. Separately, `worker/lib/maintenance.ts` reads with `pgmq.read('maintenance_jobs', 60, 1)` while `startMaintenancePolling` polls every 60s, so once full-ledger verification exceeds 60s the message becomes visible mid-processing and can be redelivered. A scoped/indexed currency lookup (leaving full-chain verification to the scheduled job) and a verification-time-aware visibility timeout would restore bounded cost. This is a deliberate whole-ledger-validation design (logged in the prior review pass), so it is recorded for focused scale work rather than reworked here.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-1-versioned-terms-tamper-evident-acceptance-store.md`
  summary: The nightly verifier's default reader fetches the ledger records and the signed head as two separate autocommitted queries, so an acceptance/erasure append committing between them can make the head one position ahead of the records and emit a false `acceptance_chain.integrity_failed` critical alert.
  evidence: `worker/lib/maintenance.ts` `dispatchMaintenanceJob` builds a reader whose `records()` and `head()` run in separate top-level `sql` statements (no shared snapshot); `verifyAcceptanceChain` then asserts `head.chain_position === records.length` and throws "head does not match the ledger" on a mismatch, which `runAcceptanceChainVerification` reports as a critical event. The request-path provider was fixed to read under one repeatable-read snapshot, but the worker's default reader was intentionally left for focused work because no real-Postgres test exercises that reader (worker tests mock both `sql` and the verifier), so a snapshot-consistency change there needs new integration coverage to verify. The concurrency window is not reachable in Story 3.1 (acceptance appends are unwired until Story 3.2), which is why this is deferred rather than blocking.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-1-versioned-terms-tamper-evident-acceptance-store.md`
  summary: Crypto-shredding a user's consent PII does not reach the plaintext identity that `account.invited` (and now, before this fix, `consent.accepted`) already wrote into the immutable `audit_events` table, so a data-subject erasure leaves recoverable email/name/mobile behind in the audit log.
  evidence: `src/features/ambassadors/dal/admin.ts` emits `account.invited` with `entity.snapshot = { fullName, email, mobile, ... }` in cleartext, and `audit_events` is immutable with no erasure path (Story 3.1's intent explicitly forbids adding audit-expiry paths). `cryptoShredAcceptancePii` destroys the per-user wrapped key and tombstones the ledger but cannot touch `audit_events`. This is a pre-existing, cross-cutting GDPR-erasure gap owned by Epic 7 (audit retention/erasure), surfaced incidentally here; Story 3.1's own `consent.accepted` PII leak was fixed in this pass by snapshotting the pseudonymous userId instead of the email. A focused Epic 7 change must decide how audit-stored PII is erased or pseudonymized without breaking the audit immutability contract.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-2-first-login-consent-card-flow.md`
  summary: `appendAcceptance` is now an orphaned second acceptance path — Story 3.2 rewired the pre-consent facade onto `acceptCurrentTermsAndActivate`, leaving `appendAcceptance` unreferenced by production code but still writing to the same append-only ledger without the profile-row lock, existence/dedup check, or activation that the new path relies on.
  evidence: Grep of `src` and `worker` (excluding tests) shows `appendAcceptance` only at its own definition (`src/features/consent/dal/acceptance.ts:44`); its former sole caller `acceptTerms` in `src/features/consent/dal/pre-consent.ts` now imports `acceptCurrentTermsAndActivate`. `appendAcceptance` takes only the `CHAIN_LOCK` advisory lock, performs no `(user, terms_version)` existence check, and never activates the profile, so any future re-wiring to it would silently bypass the single-acceptance and atomic-activation invariants Story 3.2 established. It cannot simply be deleted because `consent-store.integration.test.ts` still exercises it for concurrent-append/tombstone serialization coverage; resolution is to remove it and re-home that coverage onto the live path, or fold both into one locked path.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-2-first-login-consent-card-flow.md`
  summary: Single-acceptance-per-(user, terms_version) is enforced only in application code (SELECT-existing under a profile `FOR UPDATE` plus the chain advisory lock); there is no DB-level unique constraint, so any acceptance-append path that skips those locks can create duplicate current-version evidence the schema will accept.
  evidence: `supabase/migrations/20260716160000_story_3_1_consent_store.sql:24` creates `idx_acceptance_records_user_terms` as a NON-unique index; the only unique guard is `idx_acceptance_records_one_tombstone` (line 25, tombstones only). Story 3.2's `acceptCurrentTermsAndActivate` is provably safe (profile `FOR UPDATE` serializes per user, `CHAIN_LOCK` serializes globally, and it dedups on an existing acceptance), but the AC "exactly one current-version acceptance" rests entirely on that discipline — the orphaned `appendAcceptance` (entry above) is exactly such a lock-skipping path. A partial unique index `on acceptance_records(user_id_snapshot, terms_version_id) where record_type = 'acceptance'` would make the invariant defense-in-depth at the schema level. Deferred because the live path is currently safe and the index touches the Story 3.1 consent-store migration.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-2-first-login-consent-card-flow.md`
  summary: The legal-text sheet renders `card.legalTextMarkdown` as raw text in a `whitespace-pre-wrap` container with no markdown parser, so any markdown syntax (headings, bold, lists, links) in the binding legal terms would display as literal `#`, `**`, `[](…)` characters to the ambassador.
  evidence: `src/components/ui/sheet.tsx` renders `children` inside `<div className="whitespace-pre-wrap …">` and `src/features/consent/components/consent-card-stack.tsx:303` passes `card.legalTextMarkdown` straight through as those children — no `react-markdown`/remark step exists. The field name asserts markdown content, but rendering preserves only line breaks. Synthetic and seeded test terms use plain prose so tests pass, masking the gap. Resolution: confirm with legal/content whether `terms_versions.payload` legal text uses markdown; if so, render it through a sanitizing markdown parser, otherwise rename the field to signal plain text. Deferred rather than patched because adding a markdown pipeline is a scope addition the Story 3.2 spec does not mandate.
